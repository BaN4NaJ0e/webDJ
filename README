music playback is realized through music player daemon, so mpd and mpc should be installed and configured.

MPD Guide: http://mpd.wikia.com/wiki/Configuration
mpd --create-db
/etc/init.d/mpd restart

pyhton libs needed:

eyed3
cheetah
webpy

initial setup:
run python createMusicLib.py /path/to/music
to create the music database
change variable musicfolder in mpdPlayer.py to /path/to/music
run python webpy.py
access webinterface via http://localhost:8080/index/
